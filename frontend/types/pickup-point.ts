export interface PickupPoint {
  id: string;
  name: string;
  address: string;
  city: string;
  postalCode: string;
  phoneNumber: string;
  operatingHours: string;
  createdAt?: string;
  updatedAt?: string;
}
